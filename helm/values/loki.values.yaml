deploymentMode: SingleBinary
loki:
  auth_enabled: false              # multi-tenancy off
  commonConfig:
    replication_factor: 1
  schemaConfig:
    configs:
      - from: 2024-01-01
        store: tsdb
        object_store: filesystem
        schema: v13
        index:
          prefix: loki_index_
          period: 24h
  storage:
    type: filesystem
  limits_config:
    retention_period: 24h

singleBinary:
  replicas: 1
  persistence:
    size: 5Gi

# Disable the components that single-binary mode doesn't need
read:        { replicas: 0 }
write:       { replicas: 0 }
backend:     { replicas: 0 }
chunksCache: { enabled: false }
resultsCache: { enabled: false }