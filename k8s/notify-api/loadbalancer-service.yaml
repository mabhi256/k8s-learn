apiVersion: v1
kind: Service
metadata:
  name: notify-api-lb
  labels:
    app: notify-api
spec:
  type: LoadBalancer
  selector:
    app: notify-api
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
