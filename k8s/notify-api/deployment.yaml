apiVersion: apps/v1
kind: Deployment
metadata:
  name: notify-api
  labels:
    app: notify-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notify-api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: notify-api
    spec:
      containers:
      - name: notify-api
        image: notify-api:local-v1
        imagePullPolicy: Never
        ports:
          - containerPort: 50051
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: "100m"
            memory: "128Mi"
        livenessProbe:
          tcpSocket:
            port: 50051
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 50051
          periodSeconds: 5
          failureThreshold: 3
        lifecycle:
          preStop:
            exec:
              command: ["sh", "-c", "sleep 5"]
