apiVersion: v1
kind: Service
metadata:
  name: users-api-v1
  namespace: default
spec:
  selector:
    app: users-api
    version: v1
  ports:
    - port: 3000
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: users-api-v2
  namespace: default
spec:
  selector:
    app: users-api
    version: v2
  ports:
    - port: 3000
      targetPort: 3000