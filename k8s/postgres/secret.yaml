apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  labels:
    app: postgres
type: Opaque
data:
  # appuser / secret - same credentials as k8s/secret.yaml, so the
  # users-api Secret (DB_USER/DB_PASSWORD) keeps working unchanged.
  POSTGRES_USER: YXBwdXNlcg==
  POSTGRES_PASSWORD: c2VjcmV0
